<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platon.browser.dao.mapper.CalculateMapper">
    <select id="countTransactionOrContract"  resultType="java.lang.Long">
        SELECT
        count(*) as sum
        FROM `transaction`
        WHERE
        `receive_type` = #{receiveType}
        and `chain_id`= #{chainId}
        and (`from` = #{from} OR  `to` = #{to})
    </select>

    <select id="countBlock"  resultType="java.lang.Long">
        SELECT
        count(*) as sum
        FROM `Block`
        WHERE
        `hash` = #{hash}
        and `chain_id`= #{chainId}
    </select>

    <select id="countTransaction"  resultType="java.lang.Long">
        SELECT
        count(*) as sum
        FROM `transaction`
        WHERE
        `hash` = #{hash}
        and `chain_id`= #{chainId}
    </select>
</mapper>