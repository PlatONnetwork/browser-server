更新说明：
1. 正式对外环境（alaya主网、beta网），采取主环境 和 备份环境切换实现在线升级。
2. 其他内部环境直接升级。


关于更新步骤：
1. 停止服务（scan-agent、scan-api）
2. scan-agent升级
   2.1. 配置文件增加如下配置
	  platon:
          #锁仓最小释放金额参数生效版本
          restrictingMinimumReleaseActiveVersion: 3584
          #Redis命名空间
          redis-namespace: browser:${version}:${profile}

3. browser-api升级
   3.1. 核对以下配置值是否与底层链一致，如果不一致，以底层链为准进行修正：
      platon:
          #Redis命名空间
          redis-namespace: browser:${version}:${profile}