<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platon.browser.dao.custommapper.CustomTx721BakMapper">
  <insert id="batchInsert" parameterType="collection" useGeneratedKeys="true" keyProperty="id">
    insert into tx_721_bak
    ( `seq`, `hash`, `contract_address`, `from_address`, `to_address`, `value`)
    values
    <foreach collection="set" item="item" separator=",">
      (#{item.seq,jdbcType=BIGINT}, #{item.hash,jdbcType=VARCHAR},
      #{item.contract,jdbcType=VARCHAR}, #{item.from,jdbcType=VARCHAR},
      #{item.to,jdbcType=VARCHAR}, #{item.value,jdbcType=VARCHAR})
    </foreach>
  </insert>
</mapper>